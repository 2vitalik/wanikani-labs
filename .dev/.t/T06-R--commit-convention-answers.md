# T06 · Readback: відповіді на T05 (конвенція комітів)

`2026-09-10` · ✔️ readback

Контекст:
- джерело — чат 2026-09-10, одне повідомлення-відповідь на [T05](T05-Q--commit-convention.md); слоти `>` у T05 лишились порожні — відповіді зафіксовані тут;
- пропозиція — [T04](T04-P--commit-convention.md).

## Рішення (твої відповіді)

- **Q1 довжина** — a): заголовок ≤ 72 символи разом із суфіксом, ціль 50–60.
- **Q2 тести** — `test` (однина, стандарт Conventional Commits).
- **Q3 scope** — необовʼязковий; лише коли зміна в одному пакеті/зоні.
- **Q4 кілька тікетів** — дефіс = **діапазон**: `t03-05` означає T03, T04, T05.
- **Q5 `.dev`-зміни** — разом із кодом того ж рішення, тип за кодом.
- **Q6 трейлери агента** — лишати (`Co-Authored-By`, `Claude-Session`).
- **Q7 де фіксуємо** — у глобальному `CLAUDE.md` (`/Users/Shared/.claude/CLAUDE.md`, секція `# git`) — спільна конвенція для всіх проєктів; у проєкті — рядок у `.dev/README.md` і `CLAUDE.md` з лінком сюди.
- **Q8 перший коміт** — переписаний ок; три наявні коміти конвенції відповідають, не переписуємо.

## Мій дефолт без окремого питання (поправ `>`-ом, якщо не так)

- Несуміжні тікети — через кому без пробілу: `t03,07`; комбіновано — `t01-02,05` (так уже траплялось у `vps-infra`: `t24,26`).

## Що зафіксовано

- `/Users/Shared/.claude/CLAUDE.md` → `# git` переписано за конвенцією замість старого `t<ID>: summary`.
- `.dev/README.md` → «Конвенції», рядок «Коміти» — лінк на T04/T06; `CLAUDE.md` проєкту — короткий рядок з лінком на глобальне правило.

## Твої думки та питання

> 
