# wanikani-labs — робоча памʼять проєкту

Мета: збирати **все**, що дає WaniKani API, у MongoDB — поточний стан, повну історію версій і похідні події (ревʼю, SRS-переходи, рівні, зміни контенту) — і жити з цим далі: Telegram-бот із дайджестами у форум-топіки (зараз), веб зі статистикою (фаза 3). Продовження старого cron-скрапера (`mochi/`, файли 2022–2026) і проміжної спроби `kanji/`.

Метод — жива спека: `/Users/v4u/Dropbox/v4/Dev/My/dev-md-rules/APPROACH.md` (компактний шар DEV.md — у кожній сесії). Реєстр тікетів — [MAP.md](MAP.md), дерево вузлів — [TREE.md](TREE.md), журнал — [CHANGELOG.md](CHANGELOG.md), голос проєкту — [STATUS.md](STATUS.md).

Бачення бота (фон, не план) — [T07](.t/T07-P--bot-vision.md): сесії / денні підсумки / стріки, сповіщення про контент, нагадування, ревʼю й уроки через бота, статистика à la wkstats, multi-account / multi-user; там же — пропозиція черговості кроків.

## Конвенції

- Репо = uv-workspace монорепо, namespace `wklabs`; підпроєкти — під `packages/` ([T03](.t/T03-C--packages-layout.md)): `lib/` (ядро) · `cli/` (`wklabs`) · `bot/` (`wklabs-bot`) · `web/` (`wklabs-web`, порт 8100) · `vue/` (фаза 3, порт 5100); поза пакетами — `deploy/` (юніт, env-шаблон, runbook), `scripts/`. Дизайн і чому саме так: [T01](.t/T01-P--architecture-v1.md) (дерево там — до T03), стартові рішення: [T02](.t/T02-R--decisions-2026-08-23.md).
- Код/ідентифікатори/коментарі — EN; документація й спілкування — UA.
- Коміти робить лише Vitalik (агент — на явне прохання). Формат — глобальна конвенція (`# git` у `/Users/Shared/.claude/CLAUDE.md`): `<type>[(scope)]: <summary>, tNN[-MM][,KK]`, заголовок ≤ 72, деталі списком у тілі, у суфіксі лише тікети, які коміт створює/змінює — пропозиція [T04](.t/T04-P--commit-convention.md), рішення [T06](.t/T06-R--commit-convention-answers.md).
- Стек: Python ≥3.12, PyMongo native async (не motor), httpx, pydantic-settings, aiogram 3, APScheduler 3, typer, FastAPI; ruff + pyright + pytest (тести — на реальній локальній Mongo, БД `wanikani_labs_test`).
- Секрети — `.env` (git-ignored) локально, `shared/env` на сервері; шлях до файлу — `WKLABS_ENV_FILE`, дефолт `.env` у корені репо незалежно від cwd (як `NURE_ENV_FILE` у nure-students). В env лише `BOT_TOKEN`, `TG_ADMIN_IDS`, `WKLABS_SECRET_KEY` (Fernet-ключ токенів; копія в 1Password), `ACCESS_POLICY`, Mongo. **Акаунти WaniKani — у Mongo** (`accounts`, ключ = `wk_id[:8]`, токен зашифровано), додаються через бота (`/accounts → ➕`) або `wklabs accounts add`; доставка — `tg_chats`/`tg_routes` (`/setup` у групі чи форумі) — [T21](.t/T21-R--accounts-decisions.md).
- Проєкт №10 у реєстрі портів DEV.md. Venv — поза репо: `UV_PROJECT_ENVIRONMENT=~/.venvs/wanikani-labs uv sync --frozen` (правило DEV.md «Venv»); `pyrightconfig.json` без `venvPath` — pyright бере інтерпретатор з `uv run`.

## Вузли

Поки всі тікети в корені `.t/`; вузли зʼявляться, коли наросте (кандидати: `lib/` схема й sync, `bot/` сповіщення, `web/`).

## Стан

- ✅ v1 зібрано й перевірено локально: baseline-sync обох акаунтів (23 840 обʼєктів, 18 с, 52 запити), інкрементальний — 3 с; бот у dry-run поллить; 14 тестів, ruff/pyright чисті.
- ⬜ Деплой на vv3 — [deploy/README.md](../deploy/README.md); потрібні від тебе `BOT_TOKEN`, `TG_FORUM_CHAT_ID`, `TG_ADMIN_IDS`.
- ✅ Історія з файлів імпортована локально й звірена до одиниці ([T08](.t/T08-C--import-history.md)): 319 263 файли → 172 983 версії за 1.5 хв, `rebuild-events` → 101 271 подія за 7 с (після zip — 109 285); `fetch_smart`→Mongo вже було зроблено 08-23 (`SyncEngine`), з `kanji/` брати нічого; 16 тестів.
- ✅ `_old_data.zip` (mochi, дампи 2024-03-30…04-21) імпортовано локально командою `wklabs import-raw` (крок 3 [T10](.t/T10-P--full-data-plan.md)): рівно 7 656 нових версій, `history` = 204 479, `events` = 109 285; подій за 2024 стало 26 028. Zip лишається в mochi як джерело, більше з ним нічого робити не треба.
- ✅ Хвіст з vv1 (tar → scp → `~/Giga/data/wanikani-server`, теки `account_1/2` → мапа `1=main,2=light`) доімпортовано — [T13](.t/T13-C--server-tail-import.md): 14 225 нових версій, `history` 218 704, `events` 120 429, історія суцільна до 2026-09-10 16:43; cron на vv1 полить кожні 8 год. Рішення по T09 — [T12](.t/T12-R--old-server-answers.md); чому доповнюємо, а не з нуля — [T11](.t/T11-C--clean-slate-vs-append.md).
- ✅ `history` на vv3 (2026-09-11): restore 194 771 + 23 933 дублікати з власного sync бота (очікувано), `rebuild-events` → 218 713 версій / 120 433 подій за 11 с — на 9 версій і 4 події більше за локальну базу (бот уже сам полить), `pending_events=0`. ⬜ Через 1–2 тижні — фінальний хвіст з vv1 і вимкнення cron ([T10](.t/T10-P--full-data-plan.md)). Перевірено: subjects main/light байт-у-байт однакові, різниця лише в тому, хто зловив версію — обʼєднання в глобальній `history` і є чиста база.
- ⬜ Перенести `history` на vv3 (`mongodump`/`mongorestore`, ~93 MB на диску) + `rebuild-events` там — [deploy/README.md](../deploy/README.md) §8; після деплою бота.
- ⬜ Web + Vue (статистика).
- 💡 Чати як ціль доставки (дизайн, 2026-09-12, код не починали): реєстр `tg_chats` з трьох джерел — нативний вибір чату з приватного меню (`request_chat`, Telegram сам додає бота з правами), апдейт `my_chat_member` (додали руками) і легкий `/setup`; гілки форуму — мапа в документі чату (створені ботом + побачені; списку в Bot API нема), політика `auto | manual | General` на маршруті, розкладка `account_topics`; права бота показуються з підказкою «що зробити»; маршрут отримує `status` (помилки не мовчать), `subscribers` для спільних категорій (нуль дублів), реєстр налаштувань (`silent`, `items`) з шарами — [T22](.t/T22-P--chats-delivery-model.md); екрани `/chats` · картка чату · `📬 Delivery` → категорія → чат → гілка · `🧵 Topics` · легкий `/setup` — [T23](.t/T23-P--bot-ux-chats.md); варіанти — [T24](.t/T24-B--chats-alternatives.md); ⭐ питання — [T25](.t/T25-Q--chats-questions.md). Раунд 2 (чат 2026-09-12, [T27](.t/T27-R--chats-decisions.md)): picker + авто-реєстрація + `/setup` (Q1-a), учасник може направляти (Q3-a), Q5–Q10 за рекомендаціями; **переглянуто** — замість `layout`/`thread_auto` **пресети** (per category · per account · one topic · General) як масові дії + повністю ручна ціль на кожному маршруті, скільки завгодно маршрутів на одну гілку; `/setup` легкий, але з `⚙️ Configure here` (ті самі екрани в чаті, спільна картка з власниками); права — `can_edit` (власник · адмін бота · адмін чату в межах чату · учасник лише своє), делегування потім — [T26](.t/T26-C--chats-presets-and-rights.md); лишилось — [T28](.t/T28-Q--chats-questions-2.md). Дві фази реалізації після відповідей.
- ✅ Акаунти в Mongo замість env, multi-user через бота — реалізовано ([T18](.t/T18--accounts-v1/report.md)): `lib` (`crypto · accounts · users · delivery`, `SyncEngine` над `TokenSource`, CLI `wklabs accounts …` / `gen-key`), бот (маршрути замість топіків, `PyMongoStorage`, middleware користувачів, екрани `/accounts` · `/setup` · `/admin`); локальна база мігрована (`main → 07fff792`, `light → 27f9b9f5`); 38 тестів. ⬜ Твоє: runbook §9 на vv3 (stop → `migrate-keys` → `gen-key` → start → токени через бота → `/setup` у форумі). Дизайн [T14](.t/T14-P--accounts-model.md) (сутності `tg_users · accounts · chats · routes`, схема, токени Fernet, bootstrap з env, код), екрани й команди [T15](.t/T15-P--bot-ux-accounts.md), варіанти з аргументами [T16](.t/T16-B--accounts-alternatives.md), питання [T17](.t/T17-Q--accounts-questions.md) (⭐ Q1–Q5 блокують старт), задача [T18](.t/T18--accounts-v1/plan.md). Раунд 2 (2026-09-12) — [T19](.t/T19-C--accounts-answers.md): колекції `tg_chats`/`tg_routes` (без `tg_topics`), `TG_FORUM_CHAT_ID` геть, FSM у Mongo (`PyMongoStorage`), пропозиція мігрувати ключі `main`/`light` → `wk_id[:8]` і прибрати `WK_TOKEN__*`; відкриті ⭐ — [T20](.t/T20-Q--accounts-questions-2.md).

## Наступний крок

- Ти: відповіді `>` у [T28](.t/T28-Q--chats-questions-2.md) (права, видалена гілка, пресети в `/setup`) або «за рекомендаціями» → я ще раз проходжу дизайн на небезпечні/незручні місця, створюю задачу-теку з планом і починаю фазу 1 (реєстр чатів, picker, `/chats`, `/setup` з пресетами й `⚙️ Configure here`).
- Ти: коміт T18; локально — `uv run wklabs-bot` з `BOT_TOKEN` + свій id у `TG_ADMIN_IDS` (`.env`) → `/accounts` у приваті, `/setup` у тестовому форумі; на vv3 — runbook §9. Потім крок 2 з [T07](.t/T07-P--bot-vision.md) (сесії, денний підсумок, стрік).
- Ти: відповіді `>` у T01/T02 (якщо є правки), токен бота + chat_id форуму → `.env` → локально `uv run wklabs-bot` з реальним Telegram → потім деплой за runbook.
- Далі за деплоєм — історія на сервер (§8 runbook); локальна Mongo `kanji` більше не потрібна (дроп — на твій розсуд).
- Бот на vv3 працює з повною історією. Ти: через 1–2 тижні — фінальний tar/scp з vv1 у `~/Giga/data/wanikani-server` → я доімпортую і дам дамп лише нових версій → вимкнути cron на vv1. Далі — крок 2 з [T07](.t/T07-P--bot-vision.md) (сесії, денний підсумок, стрік).
