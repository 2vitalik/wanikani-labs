# TREE — дерево вузлів `.dev/`

<!-- автоген: `dev tree` (або `dev gen`), руками не редагувати -->

Вузол = папка без крапки; у вузла — свій `.t/` (тікети) і `README.md` (стан). Опис — з H1 README вузла. Реєстр усіх тікетів — [MAP.md](MAP.md).

- **[.dev](README.md)** — робоча памʼять проєкту · 2 тікети у корені

Системні: `.rounds/` — хроніка раундів (0 тікетів) · `.sessions/` — витяги сесій (0 md) · `.old/` — архів.
